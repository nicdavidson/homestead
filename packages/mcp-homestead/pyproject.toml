[project]
name = "mcp-homestead"
version = "0.1.0"
description = "MCP server for Homestead — exposes Manor API as Claude tools"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
mcp-homestead = "mcp_homestead.server:main"
