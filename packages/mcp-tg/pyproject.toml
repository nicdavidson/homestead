[project]
name = "mcp-tg"
version = "0.1.0"
description = "MCP server for Telegram — send files, photos, and messages"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
mcp-tg = "mcp_tg.server:main"
