[project]
name = "homestead-common"
version = "0.1.0"
description = "Shared infrastructure for homestead packages"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
